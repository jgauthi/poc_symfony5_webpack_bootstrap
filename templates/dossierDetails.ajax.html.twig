<p>
    Active: {% if dossier.active %}Yes{% else %}No{% endif %}<br>
    {% if dossier.categories is not empty %}
        Categories:
        <em>
            {% for category in dossier.categories %}
                {{ category.title }}{% if not loop.last %}, {% endif %}
            {% endfor %}
        </em>
    {% endif %}
</p>
<p class="description">{{ dossier.content|nl2br }}</p>
